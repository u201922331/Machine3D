# from model import LoadModel
import keras


def main():
    """
    Stage 1: Create and train the model
    model = LoadModel(in_path,
                      out_path,
                      batch_size,
                      )
    model.train(epochs=???, test_each=???, extend_training=???, max_extend=???)

    Stage 2: Save the current model for future use

    Stage 3: Test the resulting model against an inputted item

    """
    pass


if __name__ == '__main__':
    main()
